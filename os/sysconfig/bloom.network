[Network]
# Bloom service network — inter-service communication with outbound access
# Services that need external access (WhatsApp, etc.) use this network
# with port forwarding for local-only endpoints.
Driver=bridge
Subnet=10.89.1.0/24
Gateway=10.89.1.1
